peace_deals = {
	BUL_sue_for_peace_SER = {
		icon = GFX_decision_eng_trade_unions_support
		allowed = {
			original_tag = BUL
		}
		target_trigger = {
			FROM = {
				tag = SER
			}
		}
		available = {
			has_war_with = FROM
			OR = {
				is_faction_leader = yes
				is_in_faction = no
			}
			surrender_progress > 0.4
			NOT = {
				is_in_faction_with = GER
				has_war_together_with = GER
				is_in_faction_with = FRA
				has_war_together_with = FRA
			}
		}
		visible = {
			has_global_flag = kis_secondbalkan_war
			NOT = {
				is_in_faction_with = GER
				has_war_together_with = GER
				is_in_faction_with = FRA
				has_war_together_with = FRA
				has_global_flag = kis_secondbalkanwar_victory
				has_global_flag = kis_secondbalkanwar_lose
			}
			has_war_with = FROM
			is_subject = no
		}
		fire_only_once = yes
		ai_will_do = {
			factor = 400
		}
		complete_effect = {
			FROM = {
				country_event = {
					id = ww1_secondbalkan.5
				}
			}
			every_country = {
				limit = {
					has_war_with = ROOT
					NOT = {
						tag = TUR
					}
				}
				white_peace = ROOT
				set_country_flag = kis_secondbalkan_war_winner
			}
		}
	}
	BUL_sue_for_peace_ROM = {
		#when they did not join the balkan leauge
		icon = GFX_decision_eng_trade_unions_support
		allowed = {
			original_tag = BUL
		}
		target_trigger = {
			FROM = {
				tag = ROM
			}
		}
		available = {
			has_war_with = FROM
			OR = {
				is_faction_leader = yes
				is_in_faction = no
			}
			surrender_progress > 0.4
			NOT = {
				is_in_faction_with = GER
				has_war_together_with = GER
				is_in_faction_with = FRA
				has_war_together_with = FRA
			}
		}
		visible = {
			has_global_flag = kis_secondbalkan_war
			NOT = {
				has_war_with = SER
				is_in_faction_with = GER
				has_war_together_with = GER
				is_in_faction_with = FRA
				has_war_together_with = FRA
			}
			has_war_with = FROM
			FROM = {
				is_in_faction = no
			}
			is_subject = no
			owns_state = 77
		}
		fire_only_once = yes
		ai_will_do = {
			factor = 400
		}
		complete_effect = {
			FROM = {
				white_peace = ROOT
				transfer_state = 77
				set_country_flag = kis_secondbalkan_war_winner
			}
		}
	}
	BUL_sue_for_peace_TUR_2 = {
		icon = GFX_decision_eng_trade_unions_support
		name = sue_for_peace_TUR
		allowed = {
			original_tag = BUL
		}
		target_trigger = {
			FROM = {
				tag = TUR
			}
		}
		available = {
			NOT = {
				has_war_with = SER
				is_in_faction_with = GER
				has_war_together_with = GER
				is_in_faction_with = FRA
				has_war_together_with = FRA
			}
			any_country = {
				NOT = {
					tag = ROOT
					is_in_faction_with = ROOT
				}
				OR = {
					has_country_flag = kis_secondbalkan_war_winner
					has_country_flag = defeated_in_bw2
				}
			}
		}
		visible = {
			has_global_flag = kis_firstbalkanwar_victory
			has_war_with = FROM
		}
		ai_will_do = {
			factor = 400
		}
		fire_only_once = yes
		days_remove = 30
		complete_effect = {
			white_peace = FROM
			FROM = {
				country_event = {
					id = ww1_secondbalkan.15
					days = 7
				}
			}
		}
		remove_effect = {
			FROM = {
				country_event = ww1_secondbalkan.10
			}
		}
	}
}

BUL_change_constitution_decision = {
	BUL_rename_bulgaria = {
		available = {
			is_subject = no
		}
		fire_only_once = yes
		cost = 0
		complete_effect = {
			add_stability = 0.15			#Whatever stability you want
			drop_cosmetic_tag = yes
			set_global_flag = BUL_flag_is_tsardom
		}
	}
}

political_actions = {
	BUL_attack_tur = {
		allowed = {
			original_tag = BUL
		}
		available = {
			SER = {
				has_war_with = TUR
				NOT = {
					is_in_faction_with = BUL
				}
			}
		}
		visible = {
			has_completed_focus = BUL_lone_liberator
		}
		complete_effect = {
			declare_war_on = {
				target = TUR
				type = take_state_focus
			}
		}
	}
}
